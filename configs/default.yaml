# Default experiment configuration
seed: 42

data:
  test_size: 0.2

training:
  # CV over regularization strength
  c_grid: [0.05, 0.1, 0.2, 0.5, 1.0, 2.0, 5.0]
  cv_folds: 5

  # choose threshold that maximizes F1 on out-of-fold probs
  threshold_metric: f1

artifacts:
  out_dir: artifacts
